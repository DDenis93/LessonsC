#include <iostream>
#define _USE_MATH_DEFINES
#include <math.h>
using namespace std;
// 100, 500, 1000, 5000 RUR

int main()
{
    double r;
    cout << "Input r: ";
    cin >> r;
    cout << "S = " << M_PI * pow(r, 2) << endl;
    cout << "L = " << 2 * M_PI * r << endl;






    //int amount, k100, k500, k1000, k5000;
    //cout << "Enter withdrawel amount: ";
    //cin >> amount;

    //k5000 = amount / 5000; // целочисленный результат
    //amount %= 5000; // amount = amount % 5000;
    //// amount = amount - k5000 * 5000;

    //k1000 = amount / 1000; // целочисленный результат
    //amount %= 1000; // amount = amount % 5000;

    //k500 = amount / 500; // целочисленный результат
    //amount %= 500; // amount = amount % 5000;

    //k100 = amount / 100; // целочисленный результат
    //amount %= 100; // amount = amount % 5000;

    //cout << "k5000 = " << k5000 << endl;
    //cout << "k1000 = " << k1000 << endl;
    //cout << "k500 = " << k500 << endl;
    //cout << "k100 = " << k100 << endl;

    //cout << "to return to Your account: " << amount << endl;
}
